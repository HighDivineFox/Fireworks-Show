import Resource from "./Resource.js";

export const ResourceTracker = {
    gold: new Resource(0),
    basicFireworkCount: new Resource(999),
    flareCount: new Resource(999),
    mineCount: new Resource(999),
}